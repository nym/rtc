# RTC demo recordings

`llminerals.mp4` (or `.webm` fallback) is the canonical demo of the Llminerals
scenario — five workers harvesting tokens from a single project base, with the
occasional rate-limit error.

Regenerate with:

```bash
pnpm demo:record
```

Per-test webm artifacts land under `tests/` and are gitignored.
